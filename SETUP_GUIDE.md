# Инструкция по применению миграции

У вас есть **ДВА способа** применить миграции. Выберите любой удобный:

---

## 🌐 СПОСОБ 1: Через веб-интерфейс Supabase (САМЫЙ ПРОСТОЙ - рекомендуется!)

Занимает 3 минуты, не требует командной строки:

### Шаг 1: Откройте SQL Editor
1. Зайдите на [https://supabase.com/dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект (например `erp-mimirhead`)
3. В левом меню откройте **SQL Editor**
4. Нажмите **New Query**

### Шаг 2: Запустите ПЕРВУЮ миграцию
1. Откройте файл `supabase/migrations/20250101000000_init_kaiten.sql` из папки проекта на вашем компьютере
2. Скопируйте **ВЕСЬ** текст (212 строк)
3. Вставьте в окно SQL Editor в Supabase
4. Нажмите **RUN** (или Ctrl+Enter)
5. Должно появиться "Success. No rows returned" ✅

### Шаг 3: Запустите ВТОРУЮ миграцию
1. Откройте файл `supabase/migrations/20250102000000_add_list_tables_function.sql`
2. Скопируйте весь текст
3. Вставьте в SQL Editor
4. Нажмите **RUN**
5. Должно появиться "Success. No rows returned" ✅

### Шаг 4: Проверьте результат
1. Откройте **Table Editor** в левом меню
2. В выпадающем списке схем выберите `kaiten`
3. Вы должны увидеть 10 таблиц: users, spaces, boards, columns, lanes, card_types, property_definitions, tags, cards, card_tags

**ГОТОВО!** Теперь обновите страницу на Vercel - таблицы должны отобразиться.

---

## 💻 СПОСОБ 2: Через командную строку (для продвинутых)

### Шаг 1: Откройте командную строку в ПРАВИЛЬНОЙ папке

**⚠️ НЕ в C:\WINDOWS\system32 !**

1. Откройте проводник Windows
2. Перейдите в папку где лежит ваш проект (например `C:\Users\USER\Documents\erp_mimirhead`)
3. В адресной строке проводника напишите `cmd` и нажмите Enter
4. Откроется командная строка УЖЕ в нужной папке

Проверьте что вы в правильном месте:
```bash
dir
```

Вы должны увидеть папки: `app`, `supabase`, `lib` и файл `package.json`

### Шаг 2: Примените миграции

```bash
supabase db push
```

Если появится ошибка "Remote migration versions not found", выполните:

```bash
supabase db remote commit
```

Затем снова:
```bash
supabase db push
```

### Шаг 3: Проверьте результат

Откройте [https://supabase.com/dashboard](https://supabase.com/dashboard) → ваш проект → Table Editor

Должна появиться схема `kaiten` с таблицами.

---

## 🔍 Проверка на сайте

После применения миграций:

1. Откройте ваш сайт на Vercel (https://erp-mimirhead.vercel.app или ваш URL)
2. Подождите 1-2 минуты (Vercel может кэшировать старую версию)
3. Обновите страницу (Ctrl+F5 для принудительного обновления)
4. В разделе "Таблицы Supabase" должны появиться таблицы из схемы `kaiten`

Если таблиц нет, проверьте что в Vercel Settings → Environment Variables есть все 3 переменные:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
